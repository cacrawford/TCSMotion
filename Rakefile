# -*- coding: utf-8 -*-
$:.unshift('/Library/RubyMotion/lib')
require 'motion/project/template/ios'

$:.unshift('lib/')
require 'tcs-motion/all'

Motion::Project::App.setup do |app|
  # Use `rake config' to see complete tcs-motion settings.
  app.name = 'TCSMotion'
  app.resources_dirs = %w(resources spec/resources)
end
